<%- include('partials/header', { title: title }) %>

<section>
    <% if (favourite.length > 0) { %>
      <div id="mycart">
        <p class="mt-3 ms-3"><strong>Favourite</strong> <span style="float: right;"><a style="text-decoration: none; margin-left: -200px;" href="/remove-cartall">Remove All</a></span></p>
        <% favourite.forEach(fav => { %>
          <div class="card">
            <img src="<%= fav.imagePath %>" alt="<%= fav.product %>">
            <p><%= fav.product %></p>
            <p>₹<%= fav.price %></p>

            <a href="/deleted-favourite/<%= fav._id %>"> remove</a>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p>No favorite items found.</p>
    <% } %>
  </section>
